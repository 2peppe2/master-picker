model Examination {
  credits    Int
  module     String
  courseCode String
  scale      Scale  @default(G_OR_U)

  Course Course @relation(fields: [courseCode], references: [code])

  @@id([courseCode, module])
}

enum Scale {
  G_OR_U
  THREE_FOUR_FIVE
}
