model CoursesRequirement {
  id Int @id @default(autoincrement())

  type          CoursesType
  courses       Course[] // Implicit or
  requirementId Int
  requirement   Requirement @relation(fields: [requirementId], references: [id])
}

enum CoursesType {
  COURSES_OR // Requires any 
}
