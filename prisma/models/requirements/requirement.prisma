model Requirement {
  id Int @id @default(autoincrement())

  masterProgram String
  master        Master @relation(fields: [masterProgram], references: [program])

  creditRequirements CreditRequirement[]
  courseRequirements CoursesRequirement[]
}
