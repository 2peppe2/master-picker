model CourseMaster {
  master     String
  courseCode String
  Master     Master @relation(fields: [master], references: [program])
  Course     Course @relation(fields: [courseCode], references: [code])

  @@id(name: "courseMasterId", [master, courseCode])
}
