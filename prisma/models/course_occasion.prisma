enum Semester {
  HT
  VT
}

model CourseOccasion {
  id Int @id @default(autoincrement())
  year     Int
  semester Semester
  courseCode String
  
  periods CourseOccasionPeriod[]
  

  Course     Course @relation(fields: [courseCode], references: [code])
}
